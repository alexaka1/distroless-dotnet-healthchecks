---
"distroless-dotnet-healthchecks": patch
---

Point the container image source metadata at the package directory so Renovate can discover the existing changelog and include release notes in downstream update pull requests.
