---
"distroless-dotnet-healthchecks": patch
---

Add binary size comparison table to GitHub release notes via a file-based C# app (`build/release-binary-size.cs`), showing executable and total publish size per variant and platform with deltas from the previous release.
