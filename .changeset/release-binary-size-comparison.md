---
"distroless-dotnet-healthchecks": patch
---

Add binary size comparison table to GitHub release notes via a C# build tool (`build/ReleaseBinarySize`), showing executable and total publish size per variant and platform with deltas from the previous release.
